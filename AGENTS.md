## 分支与交付

- `master` 是唯一的集成分支，只接受工作分支合并，禁止直接在其上修改或提交，除非用户明确要求。
- 本项目明确授权：每次改动完成并验证无误后，主动提交并合并回 `master`，无需再次询问；用户另有要求时以用户要求为准。
- 合并前获取远端最新 `master` 并合并进工作分支，在工作分支解决冲突并重新验证；合并到本地 `master` 前，先同步本地 `master`。若主分支再次变化，重新同步并验证，不得跳过验证或覆盖他人改动。
- 推送工作分支，合并回 `master` 后推送 `master`，成功后清理本任务的 worktree；同步、验证或推送失败时保留现场并报告，不强行继续。

## 文档同步

- 功能、脚本、工具或仓库结构变更时，同步更新 `README.md`。
- 每次改动与决策记录到 `devlog.md`：内容、原因、时间、涉及文件。

## 提交信息

- 遵循 conventional commits 风格：`feat:` / `fix:` / `docs:` / `chore:`。
